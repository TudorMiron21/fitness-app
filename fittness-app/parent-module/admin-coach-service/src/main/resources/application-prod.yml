server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://my-postgres-postgresql.default.svc.cluster.local:5432/authorization

    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      #      ddl-auto: create-drop
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  application:
    name: adminCoachService


security:
  basic:
    enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://fitness-eureka-service:8761/eureka

minio:
  accessKey: lOTZpjM6oKrO7Xnm0Kw9
  secretKey: 6VQHWsegY8EY9xpGmwbgNGJuTEnZujDA62heEXSs
  endpoint: https://minio.minio-tenant.svc.cluster.local:443

app:
  firebase-configuration-file: fitness-app-firebase-adminsdk.json
  firebase-app-name: fitness-app

