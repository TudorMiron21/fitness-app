server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authorization
    #    url: jdbc:postgresql://my-postgres-postgresql.default.svc.cluster.local:5432/authorization

    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      #      ddl-auto: create-drop
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: adminCoachService

security:
  basic:
    enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
#      defaultZone: http://fitness-eureka-service:8761/eureka

minio:
  accessKey: 1kBuoTcahp0M0lXzThu5
  secretKey: N8MnmXJerLoDZUtRThnYMSluztugqQxK2JnfJGrT

