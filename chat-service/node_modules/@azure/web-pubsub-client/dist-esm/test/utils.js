// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
export function getAckMessagePayload(ackId, success = true, reason = "") {
    return {
        type: "ack",
        ackId: ackId,
        success: success,
        error: { name: reason, message: "message" },
    };
}
export function getGroupDataPayload(groupName, data, sequenceId) {
    return {
        sequenceId: sequenceId,
        type: "message",
        from: "group",
        group: groupName,
        dataType: "text",
        data: data,
        fromUserId: "user",
    };
}
export function getConnectedPayload(connectionId, reconnectionToken) {
    return {
        type: "system",
        event: "connected",
        userId: "user",
        connectionId: connectionId,
        reconnectionToken: reconnectionToken,
    };
}
//# sourceMappingURL=utils.js.map